using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuoyDumbController : MonoBehaviour
{
    /* 
     * Comment-out all functions so this is effectively a tag IF you don't have any actions to take
     * 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
